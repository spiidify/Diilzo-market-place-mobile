Mobile performance: in-memory token cache, AsyncStorage catalog cache, search cancellation, FlatList tuning

- Cache access token in memory to avoid SecureStore keychain reads on every request
- Add AsyncStorage TTL cache (stale-while-revalidate) for categories and brands
- Add AbortController to search to cancel stale requests on new keystrokes
- Parallelize home-screen badge fetches (chat unread + notification count)
- Fix unstable FlatList keyExtractors that included array index
- Add performance props (maxToRenderPerBatch, windowSize, initialNumToRender,
  removeClippedSubviews) to 10 previously untuned FlatLists
- Add signal support to fetchProducts and esSearchProducts for request cancellation

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
