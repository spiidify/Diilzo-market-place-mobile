# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## App architecture

This app spans the Expo mobile repository and the Django/web backend in `D:\DIILZO`. For app behavior changes, inspect both repositories and implement the change in each where appropriate, keeping server-side enforcement authoritative for business rules. Preserve pre-existing working-tree changes in either repository.

## Git workflow

After completing and verifying a fix, commit it and push it to the current GitHub branch.
